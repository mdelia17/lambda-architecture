---keyspace:dns_streaming---

nameservers
 inet address (PK) | counter packets_sent | counter packets_received
-------------------+----------------------+-------------------------

searched_categories
 text category (PK) | counter requests
------------------+-----------------

message_types
 text type (PK) | counter packets
----------------+----------------

conversations
 tuple<text,text,text> conversation_id (PK) | list<text> messages
--------------------------------+--------------------

domain_requests_and_responses
 text request_response (PK) | text type (PK) | counter requests
------------------+----------------+-----------------

errors
 inet address (PK) | counter errors
-------------------+---------------

window_domain_requests_and_responses
 timestamp start (PK) | timestamp end (PK) | text type (PK) | text request_response (PK) | counter requests
----------------------+--------------------+----------------+------------------+-----------------

window_exchanged_packets
 timestamp start (PK) | timestamp end (PK) | inet address (PK) | counter packets_sent | counter packets_received
----------------------+--------------------+----------------+-------------------------+-------------------------

window_byte_sums
 timestamp start (PK) | timestamp end (PK) | counter sum | counter count
----------------------+--------------------+-------------+-------------- 

window_message_types
 timestamp start (PK) | timestamp end (PK) | text type (PK) | counter count
----------------------+--------------------+----------------+--------------

---keyspace:dns_batch---

hosts
 inet host (PK)
 --------------

requests_responses_uniquehosts
 text type (PK) | text request_response (PK) | int count | set<inet> hosts
----------------+----------------------------+-----------+----------------

total_avg_bytes
 inet address_a (PK) | inet address_b (PK) | int total | float avg | int count
---------------------+---------------------+-----------+-----------+----------